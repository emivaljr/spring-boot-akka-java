akka {
  persistence {
    journal {
      plugin = "akka.persistence.journal.leveldb"

      leveldb {
        dir = "/tmp/persistence/journal"
      }
    }

    snapshot-store.local.dir = "/tmp/persistence/snapshots"
  }
}